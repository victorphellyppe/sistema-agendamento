import { FavoriteFilterPipe } from './favorite-filter.pipe';

describe('FavoriteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
